interface:
  display_name: "p11:share"
  short_description: "Create shareable, commentable p11 documents"
  brand_color: "#2563EB"
  default_prompt: "Use $p11:share to create a shareable, commentable document from this."

policy:
  allow_implicit_invocation: true
