interface:
  display_name: "P11"
  short_description: "Shareable, commentable agent documents"
  brand_color: "#2563EB"
  default_prompt: "Use $p11 to create a shareable, commentable document from this."

policy:
  allow_implicit_invocation: true
